spring:
  thymeleaf:
    cache: false

settings:
  cache:
    # 로컬: 5초마다 갱신 → 시트 수정 후 거의 즉시 반영
    refresh-interval-ms: 5000

logging:
  level:
    com.campswing: DEBUG
    org.springframework.web: INFO
    org.springframework.security: INFO
